[project]
name = "pyfetch"
version = "0.1.0"
description = "Fast Linux system fetch CLI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "psutil>=7.2.2",
    "rich>=15.0.0",
]

[project.optional-dependencies]
nvidia = ["nvidia-ml-py>=12.535.133"]
amd = ["pyamdgpuinfo>=2.0.0"]